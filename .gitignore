# Build / env
build/
.venv/
*.exe
*.zip

# Trained models — kept local (regenerated by retraining)
*.onnx
*.ncnn.bin
*.ncnn.param
src/models/

# Audio assets (WAV) — kept local (re-recorded as needed)
*.wav

# Datasets / large captures — kept local
test/
pi_debug_frames/
*.mp4

# demo/ = local working folder for raw captures; curated assets live in docs/ + README_assets/
demo/*
